<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns= "sap.m" xmlns:l="sap.ui.layout"
	xmlns:f="sap.ui.layout.form"> 
    <Dialog title="Details"> 
        <content>

        <!-- <f:SimpleForm id="idSimpleForm1">
			<f:content>
				<Label text="Name" />
				<Text text="{/SampleProperty/Name}"/>
                <Label text="Age" />
				<Text text="{/SampleProperty/Age}"/>
                <Label text="Gender" />
				<Text text="{/SampleProperty/Gender}"/>
			</f:content>
		</f:SimpleForm> -->

        <f:SimpleForm id="idSimpleForm">
			<f:content>
				<Label text="Name" />
				<Text text="{DetailsModel>/SampleProperty/Name}"/>
                <Label text="Age" />
				<Text text="{DetailsModel>/SampleProperty/Age}"/>
                <Label text="Gender" />
				<Text text="{DetailsModel>/SampleProperty/Gender}"/>
			</f:content>
		</f:SimpleForm>
        </content>
        <endButton> 
            <Button text="close" press="handleCloseDialog"/>
        </endButton>
    </Dialog>
</core:FragmentDefinition>